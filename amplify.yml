version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install -g sass
    build:
      commands:
        - sass scss/style.scss css/style.css
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []